
        <h1 class = "text-3xl">Editing Product</h1>

        <%= render 'formedit', product: @product %>


        <%= link_to 'Back', products_path, class:"bg-blue-500 hover:bg-blue-700 text-white text-center font-bold py-2 px-4 rounded"  %>

        <%= link_to 'Add a new variant', new_product_variant_path(pid: @product.id), class:"bg-green-500 hover:bg-green-700 text-white text-center py-2 px-5 rounded" %> 
